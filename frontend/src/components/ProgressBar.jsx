import "./ProgressBar.css"

const ProgressBar = ({total, now}) => {
    return (
        <section className="progressbar">
            progressbar 자리
        </section>
    );
};

export default ProgressBar;